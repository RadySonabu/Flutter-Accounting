import 'package:app/core/crud/crud.dart';

class JETrasactionTypeService extends BaseCrudAPI {
  @override
  Future getList(baseUrl, endpoint, modelSchema) {
    // TODO: implement getList
    return super.getList(baseUrl, endpoint, modelSchema);
  }

  @override
  Future getItem(baseUrl, endpoint, modelSchema) {
    // TODO: implement getItem
    return super.getItem(baseUrl, endpoint, modelSchema);
  }

  @override
  Future createItem(Map request, baseUrl, endpoint, modelSchema) {
    // TODO: implement createItem
    return super.createItem(request, baseUrl, endpoint, modelSchema);
  }

  @override
  Future updateItem(Map request, baseUrl, endpoint, modelSchema) {
    // TODO: implement updateItem
    return super.updateItem(request, baseUrl, endpoint, modelSchema);
  }
}
